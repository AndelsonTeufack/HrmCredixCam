
spring:
  datasource:
    username: postgres
    password: Teufack01
    url: jdbc:postgresql://localhost:5432/Credix HRM
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081